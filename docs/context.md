# FitAl 项目状态(context)

> 活文档:当前状态、最近改动、待决策。每次有意义的改动后更新;保持精简,旧内容压缩进历史摘要。
> 稳定内容(架构决策/契约/工作规则)在 `../CLAUDE.md`,任务看板在 `todo.md`,产品定义在 `product.md`。

## 任务清单

见 **`todo.md`**(唯一任务看板:已办/进行中/待办/等用户)。

## 当前状态(2026-07-06)

**核心链路已跑通并真机验证**:`POST /chat` 一句话 → LLM 洗数据(structured output+微循环)→ 查表/公式计算 → raw 入库 → AI 归组判断 → new 层(meals/sessions)→ SSE 返回记录卡片+模板回执。

- 已落地:七张表(迁移 0001-0004 均已应用云端 PG)、food.json 1595 条、met.json 80 条、lookup 全部公式、解析器(四级优先级/补油/组间隔计时/表外归档)、AI 归组+起名(无阈值常量,解析时附开放顿/场摘要逐条判断归属并输出组名;AI 没给名按时段兜底,已有名不被兜底覆盖)、AI 记忆("记住"即学:自定义食物/叫法/习惯;全量注入解析 prompt;每日首次请求顺带巩固,失败静默不影响主链路)、修正/删除接口(重算+聚合增量重算+纠正记忆即时学)
- 时间约定:存储一律 aware UTC(use_tz=True),本地时间(Asia/Shanghai)仅在注入 prompt/展示时转换
- LLM:任意 OpenAI 兼容端点,`.env` 四项(LLM_BASE_URL/LLM_MODEL/LLM_API_KEY 可空/LLM_EXTRA_BODY);当前 deepseek-v4-flash,思考模式经 extra_body 关闭
- 展示接口:/days 日汇总(只读 new 层合计,顿/场挂明细,归属按开始时间,当天没称重体重为空)+ /weights 体重曲线(默认 30 天,从早到晚);零 AI 调用。/users 暂缓(形式待定)
- 测试 62 个全过(内存 SQLite);演示用户 id=1(昵称"演示",云端库,含若干测试记录与记忆,后续可清;其中一场旧硬拉训练无名——起名功能上线前的数据,展示为 null 属预期)
- 已知解析瑕疵记录在案:"又一组"类表述曾拆两条(已修);牛肉面整体估算未走拆解(可打磨)

**下一步(新会话从这里开始)**
1. /users 用户接口(形式待定,先与用户对齐)或直接进真实使用打磨(错例驱动)
2. 之后按看板:读代理 → Web 前端(动工前先与用户对齐功能形态)
3. 沟通约定(2026-07-04 用户定):向用户讲方案/进展一律纯中文自然语言描述流程,不出现文件名/函数名/参数名

## 待决策

1. Web 端具体产品功能形态(页面/交互细节)—— 开做 Web 前与用户对齐(技术栈已定稿)
2. 流式语音转文本的云端供应商 —— 用户调研豆包(火山引擎)价格中,结论回来再定

## 历史摘要

- 2026-07-02:项目启动,定稿三文档体系与全部架构决策;后端骨架(FastAPI+Tortoise+云端 PG);数据表生成与校验(排除 82 条坏数据,找回 20 条主食);七张表落库;lookup 公式模块。期间用户多次纠偏:节奏放慢逐块过设计、契约类产物实现前须过目、不举例不类比、表结构拆表不用 JSONB。
- 2026-07-03:解析器(structured output+微循环 harness+四级优先级);LLM 改通用 OpenAI 兼容接入;真机联调四场景全对。
- 2026-07-04:/chat 端点(SSE)+ 时区统一 UTC(修真机 bug);归组由固定阈值改为 AI 判断(用户定);决策入档:不做思考过程展示;AI 起名并入归组调用(时段名兜底);AI 记忆("记住"即学/注入/每日巩固);修正/删除接口(重算+纠正即时学)。三项均真机验证通过。
- 2026-07-06:notes 整理——backend-qa 补修正/删除条目(Q27,后续编号顺延至 Q31);建 frontend-qa 占位骨架与 notes/README。展示接口 /days + /weights 上线(口径用户拍板:没称重返回空、曲线默认30天),62 测试全过+真机验证;/users 暂缓。前端定位修正(用户定):设计从 iOS app 出发、Web 为轻量快速实现、纯手机宽度不做 PC 适配、图标库不用 emoji。Web 技术栈定稿(React+Vite+Tailwind 自绘+Lucide+ECharts);/users 不做(单人期前端固定用户,用户定)。
- 2026-07-07:iOS 本地版构想否决(用户定):所有端一律走服务器后端,主流服务型架构;手机存储将来最多当缓存。

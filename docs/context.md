# FitAl 项目状态(context)

> 活文档:当前状态、最近改动、待决策。每次有意义的改动后更新;保持精简,旧内容压缩进历史摘要。
> 稳定内容(架构决策/契约/工作规则)在 `../CLAUDE.md`,任务看板在 `todo.md`,产品定义在 `product.md`。

## 任务清单

见 **`todo.md`**(唯一任务看板:已办/进行中/待办/等用户)。

## 当前状态(2026-07-07)

**核心链路已跑通并真机验证**:`POST /chat` 一句话 → LLM 洗数据(structured output+微循环)→ 查表/公式计算 → raw 入库 → AI 归组判断 → new 层(meals/sessions)→ SSE 返回记录卡片+模板回执。

- 已落地:七张表(迁移 0001-0004 均已应用云端 PG)、food.json 1595 条、met.json 80 条、lookup 全部公式、解析器(四级优先级/补油/组间隔计时/表外归档)、AI 归组+起名(无阈值常量,解析时附开放顿/场摘要逐条判断归属并输出组名;AI 没给名按时段兜底,已有名不被兜底覆盖)、AI 记忆("记住"即学:自定义食物/叫法/习惯;全量注入解析 prompt;每日首次请求顺带巩固,失败静默不影响主链路)、修正/删除接口(重算+聚合增量重算+纠正记忆即时学)
- 时间约定:存储一律 aware UTC(use_tz=True),本地时间(Asia/Shanghai)仅在注入 prompt/展示时转换
- LLM:任意 OpenAI 兼容端点,`.env` 四项(LLM_BASE_URL/LLM_MODEL/LLM_API_KEY 可空/LLM_EXTRA_BODY);当前 deepseek-v4-flash,思考模式经 extra_body 关闭
- 展示接口:/days 日汇总(只读 new 层合计,顿/场挂明细,归属按开始时间,当天没称重体重为空)+ /weights 体重曲线(默认 30 天,从早到晚);零 AI 调用。/users 暂缓(形式待定)
- 测试 62 个全过(内存 SQLite);演示用户 id=1(昵称"演示",云端库,含若干测试记录与记忆,后续可清;其中一场旧硬拉训练无名——起名功能上线前的数据,展示为 null 属预期)
- 已知解析瑕疵记录在案:"又一组"类表述曾拆两条(已修);牛肉面整体估算未走拆解(可打磨);**成分双重计算**(2026-07-07 真实错例):"毛毛虫面包120克,奶油占其中"被记成面包120g+奶油30g(=150g 的热量)——拆解时未从总克重中扣除成分克重,修法:解析规则加"各成分克重之和=用户报的总克重",打磨阶段处理

- Web 前端①②步完成:骨架(React+Vite+Tailwind v4+Lucide+ECharts,开发期 /api 代理到本地 8000)+ 主页(顶部日期切换/汇总条、饮食运动分区卡片、展开明细带来源标注、底部输入框);已接真后端——/days 拉真数据(空日态/错误重试/加载态),/chat SSE 发送(回执气泡,记录后跳回今天刷新);固定用户 id=1。真机全链路验证:"喝了一杯牛奶,大概250毫升"→ 纯牛奶(代表值,全脂)250g 查表 163 千卡,归组"晚间加餐"
- 已知小刺:预览中首次点发送未触发(疑与热更新竞态相关,重试即通;真机待观察)

- Web 第三步完成:明细改删抽屉(改份量后端重算/改热量转自报/删除二次确认,组数据联动刷新)+ 体重曲线抽屉(ECharts 按需加载,近30天);真机验证通过
- 顶部英雄区改版(2026-07-07 用户定):左两颗呼吸圆球(净摄入=摄入−基础代谢−运动净耗,负值自动翻"净消耗"变橙;消耗=基础代谢+运动)+右体重动态卡(今天/上次对比+迷你曲线,点开大曲线);今天基础代谢按已过时间前端本地折算(每分钟自刷,零轮询);/days 契约加 bmr_kcal、burn_net_kcal 两字段(64 测全过)。To C 体验标准入档 CLAUDE.md;整体 UI To C 化打磨列入待办
- 打磨方向已与用户对齐(2026-07-07):①餐次时段制——明说哪顿归哪顿,否则按固定时段(早餐5-10/午餐10-15/下午茶15-17/晚餐17-22/其余),组名=时段名,取消 AI 起花名,餐次归组去 AI 化;运动场次仍 AI 归组;②菜层+克重守恒硬校验——拆解输出带菜名+总克重+成分清单,代码校验成分和=总克重不符打回,堵"毛毛虫面包双算"类错误;展示三层餐次→菜→食材。两项均契约先行

- 餐次时段制上线(2026-07-08):解析新增 meal_slot 提取(仅明说才填),餐次归组纯代码(时钟落槽/同槽合并/跨天不串),AI 归组与起名仅保留运动场次;66 测全过+真机双路径验证。历史餐次旧名(如"牛肉面午餐")保留不迁移,raw 可随时重算

**✅ 菜层工程包完成(2026-07-08,原 docs/plan-dish-layer.md,结论已折叠回 CLAUDE.md 后归档删除)**
四个真机验收案全过:①毛毛虫面包120克奶油30克→面包体90g+奶油30g=120g 守恒,双算修复(经真实前端全链路);②生米100克→稻米346千卡 / 米饭100克→米饭(蒸)116千卡,形态裁决生效;③"记住蛋白粉一勺30克120千卡"→每100克400入库,缺克数→不入库回执提示补(回执不再冠"已记录");④脚本直插2条每100克数据→"吃了200克希腊酸奶"命中自定义118千卡(提示词无任何清单)。验收测试数据已全部清理。分步记录:
- 第一步·数据层迁移 ✅(2026-07-08):user_foods 改每100克口径(unit 列删除、加可空形态列,与官方表同构),food_records 加可空所属菜名标签;迁移 0005/0006 已应用云端,旧按份演示数据(蛋白粉1条)已删。"记住"链路同步新口径:必须给克数+热量,代码换算每100克入库,缺克数不入库、回执提示补克数;自定义查表改"每100克×克数"与官方表同算法;解析输出的份数字段删除。69 测全过。名字清单注入提示词的旧捷径留待第三步连同"原话叫法"字段一起拔
- 第二步·形态歧义簇 ✅(2026-07-08,用户改定缩水版取代全表打标):不动 food.json,独立小数据文件人工圈8个高频簇(米/面条/木耳/香菇/海带/玉米/枣/百合,簇内形态间热量差均≥2倍;花生生炒仅差3%不算陷阱已剔除),簇内条目标形态(生/熟/干/水发/即食封闭词表);查表模块启动加载,可按名查全簇与成员形态,别名先归标准名再进簇。5个新单测把数据健康写成规格,74 测全过
- 第三步·解析结构+硬校验 ✅(2026-07-08):拆解类输出改"菜"结构(菜名+总克重+成分清单,成分逐条入 raw 带同名菜标签,菜合计现算不建表);克重守恒硬校验(成分和=总克重±5%,超差带差额打回,重试用尽违规菜整体降级估算一条标 llm_estimated);每条食物带原话叫法+标准名双名,自定义表按原话精确命中(键归一化与官方表同规则),就算标准名命中官方表也是自定义优先;形态仅明说才填(封闭词表),明说+命中歧义簇由代码在簇内换条目(零AI),没明说+命中簇由微循环第二轮连用户原话封闭裁决(与未命中重映射合并一次调用);提示词拔掉自定义名字清单注入与生熟叮嘱(结构化取代,自定义键集合只在代码侧用),新增高热量调味显式成条(糖/麻酱/沙拉酱/蚝油,盐醋酱油忽略;表外调味必须带估算,防止错用油的每克9千卡兜底)与个人单位靠记忆换算两条正当语言职责。86 测全过;真机冒烟:毛毛虫面包120克奶油30克 → 面包体90g+奶油30g=120g 守恒,双条带菜标签,老双算错例修复
- 第四步·查表 ✅清空(2026-07-08 用户砍定):自定义表只做原话精确命中(第三步已实现),不做模糊召回;不做内存缓存/Redis,每次请求直接查库——几千条索引查询毫秒级,缓存属过度设计
- 第五步·展示接口 ✅(2026-07-08):日汇总餐次明细分两种 items——带菜标签的成分归拢成菜(type=dish:菜名+总克重+合计热量+成分列表,合计现算不存表,同顿同名菜合并,菜的位置=成分首现位置),无标签平铺单品(type=food,原字段不变);老数据零迁移天然兼容。88 测全过
- UI 打磨·食物名展示 ✅(2026-07-08 晚,用户定,UI To C 化打磨第一步):官方成分表书面名太术语化(如"牛肉(代表值,瘦,fat3g)"),列表只删纯编制术语(代表值/fatXg/标一~四/特等),保留生熟形态与部位等有用描述,完整学名收进明细抽屉("成分表名"行);纯展示层不动数值,1595条中89条精简。真机验证稻米/牛肉(瘦)显示与详情学名均正确
- 静默刷新 ✅(2026-07-08 晚,用户定):改/删/发送后的重拉不再清空列表闪"加载中",旧数据挂着原地换数值,展开状态与入场动画不受影响;仅首次进入与切换日期走完整加载态。真机验证:发送/抽屉删除全程无加载闪烁、晚餐卡片保持展开。来源标签口径经用户确认:谁出的数标谁——改克数表内重算保持"查表",直接改热量采用用户数转"你报的"
- 错例打磨第一批 ✅(2026-07-08 晚,用户真实使用当天报错):①复述被拆两条("100克虾仁,水煮的虾仁"记成两份)→ 提示词"补充说明不是新记录"规则从运动侧推广到食物侧(与"又一组"同先例,属语言理解职责),真机复测只出一条;②做法热量差(水煮 vs 红烧)→ 架构本已正确(差异=油糖显式条目),缺口是补油触发只枚举炒/煎/炸,放宽到红烧/油焖/干煸/爆等按常识用油的做法、红烧/糖醋补糖,真机复测红烧虾仁=虾仁199+油90+糖20;③菜行不能删 → 前端菜行加整删(行尾垃圾桶,两击确认3秒自动收回,删整菜=逐条删成分,所在顿重算),真机验证通过。88 测全过
- 第六步·前端三层展示 ✅(2026-07-08):餐次卡片内"菜"独立成行(菜名+总克重+成分数+合计,可展开成分,成分缩进带来源标注),单品照旧;成分点击复用现有改删抽屉,改克数后菜行/顿合计联动重算(真机验证 30→15→30 克来回,579→447→579);组头"条"数按 raw 记录数计;"按份记"文案随口径删除。类型检查+lint 干净,控制台零报错

**之后**
1. UI 整体 To C 化打磨 / 真实使用打磨(错例驱动)→ 读代理 → 流式语音(厂商用户已选定待告知)
2. 沟通与执行约定已升入 CLAUDE.md 工作规则第3条(2026-07-08):每步先讲后做、纯中文流程不报文件名/函数名/参数名、重要取舍先问

## 待决策

1. Web 端具体产品功能形态(页面/交互细节)—— 开做 Web 前与用户对齐(技术栈已定稿)
2. 流式语音转文本的云端供应商 —— 用户调研豆包(火山引擎)价格中,结论回来再定

## 历史摘要

- 2026-07-02:项目启动,定稿三文档体系与全部架构决策;后端骨架(FastAPI+Tortoise+云端 PG);数据表生成与校验(排除 82 条坏数据,找回 20 条主食);七张表落库;lookup 公式模块。期间用户多次纠偏:节奏放慢逐块过设计、契约类产物实现前须过目、不举例不类比、表结构拆表不用 JSONB。
- 2026-07-03:解析器(structured output+微循环 harness+四级优先级);LLM 改通用 OpenAI 兼容接入;真机联调四场景全对。
- 2026-07-04:/chat 端点(SSE)+ 时区统一 UTC(修真机 bug);归组由固定阈值改为 AI 判断(用户定);决策入档:不做思考过程展示;AI 起名并入归组调用(时段名兜底);AI 记忆("记住"即学/注入/每日巩固);修正/删除接口(重算+纠正即时学)。三项均真机验证通过。
- 2026-07-06:notes 整理——backend-qa 补修正/删除条目(Q27,后续编号顺延至 Q31);建 frontend-qa 占位骨架与 notes/README。展示接口 /days + /weights 上线(口径用户拍板:没称重返回空、曲线默认30天),62 测试全过+真机验证;/users 暂缓。前端定位修正(用户定):设计从 iOS app 出发、Web 为轻量快速实现、纯手机宽度不做 PC 适配、图标库不用 emoji。Web 技术栈定稿(React+Vite+Tailwind 自绘+Lucide+ECharts);/users 不做(单人期前端固定用户,用户定)。
- 2026-07-07:iOS 本地版构想否决(用户定):所有端一律走服务器后端,主流服务型架构;手机存储将来最多当缓存。

# FitAl 任务看板

> 唯一的待办/已办清单,每次有进展就更新。当前状态细节与待决策见 `context.md`。

## ✅ 已完成

- [x] 三文档体系 + 全部设计定稿:架构主流程、七张表、chat 契约 v3、读写不对称、LLM 选型(2026-07-02)
- [x] 后端骨架:FastAPI + Tortoise 连云端 PG,`/health` 可用,内置迁移体系跑通
- [x] 静态数据:food.json 1575 条(清洗 + 三道校验,排除 82 条坏数据)、met.json 50 条起始集
- [x] 七张表落库:迁移 0002 已应用云端;单测切内存 SQLite
- [x] lookup.py:查表两级匹配 + 全部热量公式,14 个单测全过
- [x] 数据补全:旧版独有 20 条主食(馒头/花卷/油条/煮面条等)质检后并入,food.json 1575→1595;查表键标点归一化。面包/饺子/包子源头无数据,走估算兜底
- [x] met.json 补杠铃五动作(卧推/杠铃深蹲/硬拉/推举/杠铃划船,MET 6.0),50→55 条;组间隔公式精确化入档 CLAUDE.md
- [x] 估算策略定稿存档:表外食物"AI 定结构,表定数值";表外力量动作归强度档、动作名照存——均已写入 CLAUDE.md 决策 2
- [x] met 表扩到 80 条(常见健身房动作按肌群补齐,复合 6.0/孤立 3.5)
- [x] 消耗口径双存(主显总耗,净耗同存)+ 自定义食物表 user_foods:迁移 0003 已应用云端,查表优先级=自报>自定义>标准表>估算
- [x] 解析器离线部分:schema 强约束输出 + 微循环 harness(校验器打回重出/候选重映射/估算降级)+ build_records 全部计算;候选召回改字符重合度(修"鸡胸肉""西红柿"两处召回盲区);27 测试全过
- [x] LLM 接入改通用 OpenAI 兼容(base_url/model/key 可空/extra_body 四项配置);真机联调通过:卧推组、鸡胸肉+一碗米饭映射、番茄炒蛋自动补油+体重、猪脚饭估算兜底,四场景全对
- [x] /chat 端点上线(SSE 事件流:records+reply 模板回执):解析→raw 入库→规则归组(餐次45min/场次20min)增量维护 new 层;curl 全链路演示通过,组间隔计时经真机验证
- [x] 时区修正:全链路改 aware UTC(use_tz=True),本地时间仅在注入 prompt/展示时转换——修复"间隔差8小时"真机bug;首个解析错例("又一组"拆两条)已修
- [x] 不做思考过程展示(入档决策9);迁移0004:自报热量/按份食物的可空字段

## 🔨 进行中

- [ ] Web 前端(纯手机宽度,设计从 iOS 出发):①骨架+主页静态版 ✅ → ②接通后端 ✅ → ③明细改删抽屉+体重曲线面板 ✅(2026-07-07:点明细弹底部抽屉,改克数/时长/负重/次数/热量按后端规则重算,删除二次确认,组总数与起止时间联动刷新;点体重弹曲线抽屉,ECharts 按需加载,近30天单序列折线。真机验证:黄瓜10g→100g 热量2→16、删除后组回原状)→ 待:真实使用打磨

## ✅ 已完成(续)

- [x] 归组改 AI 判断(2026-07-04 用户定,取代时间阈值常量):解析时附带当天开放顿/场摘要,AI 逐条判断延续/新开,零额外调用;真机三场景验证(12小时后新开一顿/紧随归入/26分钟后延续同场——最后一例旧阈值会判错)
- [x] AI 起名:归组同一次调用顺带输出顿/场展示名,代码写入;AI 没给名按开始时间时段兜底(早餐/午餐/晚间训练等);摘要携带现名供 AI 迭代改名;40 测试全过 + 真机验证("牛肉面午餐"/"卧推训练")
- [x] ai_memories 记忆:解析输出新增 user_food_def/memory 两种条目("记住"即学,自定义食物重复记住=更新);全量记忆注入解析 prompt;每日首次请求顺带巩固(整层替换,空结果/失败均不删,单测覆盖);48 测试全过 + 真机验证(记蛋白粉→按份入库;记"一碗饭=150克"→后续解析生效)
- [x] 修正/删除接口(PATCH/DELETE):改克数→表内全套重算/表外按比例缩放;改时长→按 MET 重算;直接改热量→采用并转自报;删除/修改触发所在顿/场增量重算,组空即删;改删 AI 估算记录时模板生成纠正记忆(kind=correction);56 测试全过 + 真机验证(删补的油、改菜克数,餐次总数与纠正记忆均正确)
- [x] 展示接口 /days + /weights(只读 new 层,零 AI 调用):日汇总读聚合层合计、顿/场挂明细、归属按开始时间、当天没称重体重返回空(用户定);体重曲线默认 30 天(用户定)从早到晚;62 测试全过 + 真机验证(演示用户 7/4 真数据、空日全零、404)

- [x] 餐次时段制(2026-07-08,用户定,取代餐次 AI 归组):明说哪顿归哪顿(解析提取 meal_slot),否则时钟落槽(早5-10/午10-15/下午茶15-17/晚17-22/其余);组名=时段名,一天每槽一组同槽合并,跨天不串;场次仍 AI 归组。66 测全过 + 真机验证(凌晨说"早餐吃了水煮蛋"→早餐槽,没明说的牛奶→其余槽,删除后组自动消失)

## 📋 待办(按顺序)

- [ ] UI 整体 To C 化打磨:现有页面(区头/卡片/抽屉/输入框)按体验标准过一遍质感与动效——用户点名整体偏素,不止顶栏
- [ ] 真实使用打磨(错例驱动)
- [ ] 读代理(自然语言查数,周报是查询的一种)→ 澄清式反问
- [ ] 设置页(第二批,配套新后端接口):改身体档案 + 查看/删除自定义食物与 AI 记忆;模型配置界面永久不做(2026-07-07 用户定)
- [ ] 程序内语音输入(流式 STT,WebSocket,不做整段往返;走后端转发云厂商,厂商用户已选定待告知)—— 前端阶段
- [ ] iOS 原生前端(SwiftUI,主产品,不设 deadline)
- [ ] (远期)多用户时补 /users 列表接口(选人);建用户维持管理员手工

## ⏳ 等用户

- [ ] 流式语音云厂商:用户已选定,等用户告知厂商与开通信息(前端阶段接入)
